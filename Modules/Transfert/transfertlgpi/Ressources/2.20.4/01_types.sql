create or replace type migration.type_tab_id is table of integer;
/
create or replace type migration.type_tab_varchar2_2 is table of varchar2(2);
/
create or replace type migration.type_tab_varchar2_3 is table of varchar2(3);
/
create or replace type migration.type_tab_varchar2_30 is table of varchar2(30);
/
create or replace type migration.type_tab_varchar2_50 is table of varchar2(50);
/
create or replace type migration.type_tab_char_1 is table of char(1);
/
create or replace type migration.type_tab_boolean is table of char(1);
/
create or replace type migration.type_tab_char_5 is table of char(5);
/
create or replace type migration.type_tab_number_5_2 is table of number(5,2);
/