select cast(prd.t_produit_id as varchar2(50)),
       prd.codecip,
       prd.codecip7,
       prd.designation,
       prd.prixachatcatalogue,
       prd.prixvente,
       prd.baseremboursement,
       prd.etat,
       prd.delaiviande,
       prd.delailait,       
       prd.gereinteressement,
       prd.commentairevente,
       prd.commentairecommande,
       prd.commentairegestion,
       prest.code,
       prd.geresuiviclient,
       tva.tauxtva,
       prd.liste,
       prd.tracabilite,
       prd.lotachat,
       prd.lotvente,
       prd.stockmini,
       prd.stockmaxi,
       prd.pamp,
       prd.tarifachatunique,
       prd.profilgs,
       prd.calculgs,
       prd.nbmoiscalcul,
       prd.gerepfc,
       prd.soumismdl,
       prd.conditionnement,
       prd.moyvte,
       prd.umoyvte,
       prd.datedernvte,
       prd.contenance,
       prd.unitemesure,
       prd.prixachatremise,
       prd.veterinaire,
       prd.servicetips,
       prd.typehomeo,
       prd.t_repartiteur_exclusif_id,
       cast(prd.t_codif1_id as varchar2(50)),
       cast(prd.t_codif2_id as varchar2(50)),
       cast(prd.t_codif3_id as varchar2(50)),
       cast(prd.t_codif4_id as varchar2(50)),
       cast(prd.t_codif5_id as varchar2(50)),
       cast(prd.t_codif6_id as varchar2(50)),
       prd.dateperemption,
       prd.id_product_bdm,
       prd.id_package_bdm
from erp.t_produit prd,
     erp.t_prestation prest,
     erp.t_tva tva
where prd.t_prestation_id = prest.t_prestation_id
  and prd.t_tva_id = tva.t_tva_id
  and prd.materiel_location = '0'
  and prd.d_del is null
  and reference <> '1'